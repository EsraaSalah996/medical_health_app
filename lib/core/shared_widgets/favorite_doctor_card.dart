import 'package:flutter/material.dart';

class FavoriteDoctorCard extends StatelessWidget {
  const FavoriteDoctorCard({super.key});

  @override
  Widget build(BuildContext context) {
    return Column(children: [Text('favorite card ')]);
  }
}
